import { USER_ACTION_TYPES } from './user.reducer.types';
import { checkUserSession, signInSuccess, signInFailed } from './user.action';
import { TSignInInput } from '@/pages/auth/sign-in/sign-in';

export function* signIn({ username, password }: TSignInInput) {
  try {
  } catch (error) {}
}
